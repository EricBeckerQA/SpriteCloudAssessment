# Dependencies
node_modules/

# Environment / secrets — never commit real credentials
.env
.env.local

# Playwright artifacts (regenerated every run, not source)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.log
